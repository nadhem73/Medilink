spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ambulance_db
    username: postgres
    password: postgres
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.medilinktunisia.ambulanceservice: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
