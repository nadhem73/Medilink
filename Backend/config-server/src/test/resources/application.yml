spring:
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/configurations/
  security:
    user:
      name: configadmin
      password: test

eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false
