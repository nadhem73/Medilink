name: Frontend CI - Angular

on:
  pull_request:
    branches: [ "main" ]
    paths:
      - 'Frontend/**'
      - '.github/workflows/**'
  workflow_dispatch:

jobs:
  quality:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: Frontend/medilink-angular

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: Frontend/medilink-angular/package-lock.json

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      - name: Lint check
        run: npx ng lint
        continue-on-error: true

      - name: Run tests with coverage
        run: npm test -- --watch=false --browsers=ChromeHeadless --code-coverage

      - name: Build Angular app
        run: npm run build -- --configuration production

      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        with:
          name: frontend-coverage
          path: Frontend/medilink-angular/coverage/
          retention-days: 7
          if-no-files-found: warn

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: medilink-angular-dist
          path: Frontend/medilink-angular/dist/
          retention-days: 7
          if-no-files-found: warn
